<%- include("header")-%>
    <div class = "box" id ="heading">
        <h1><%=listTitle%></h1>
    </div>
   <div class="box">
    <!-- <%for(var i = 0; i < newItems.length; i++){%>
    <div class="item">
        <input type="checkbox">
        <p><%=newItems[i]%></p>
    </div>
    <%}%> -->
    <%newItems.forEach(function(newItem){%>
        <form action = "/delete" method = "post">
            <div class="item">
                <!-- onchange help submit the form when the box is checked -->
                <input type="checkbox"  name ="checkbox" value = "<%=newItem._id%>" onChange="this.form.submit()">
                <p><%=newItem.name%></p>
            </div>
            <input type ="hidden" name ="list" value = "<%=listTitle%>"> </input>
        </form>
    <%})%>
    <form action="/" method="post" class = "item">
        <input type = "text" name = "newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name ="listTitle" value=<%= listTitle%>>+</button>
    </form>
   </div>
<%- include("footer")-%>
   

